-- select specific column data
select title, author from books;

-- show only member names and their membership status
select name, status from members;

-- show loan dates and due dates only
select loan_date, due_date from loans;
